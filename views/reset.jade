extends layout

block content
  form(method='POST')
    p
    legend Đổi mật khẩu
    .form-group
      label(for='password') Mật khẩu mới
      input.form-control(type='password', name='password', value='', autofocus=true)
    .form-group
      button.btn.btn-primary(type='submit') Đổi mật khẩu
      a.btn.btn-link(href='/') Đăng nhập
